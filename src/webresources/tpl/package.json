{
  "name": "tpl-webresources",
  "version": "0.1.0",
  "private": true,
  "description": "Web resources (TypeScript) do template D365 CE — prefixo tpl.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@types/xrm": "^9.0.0",
    "typescript": "^5.4.0"
  }
}
