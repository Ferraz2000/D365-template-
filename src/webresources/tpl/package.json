{
  "name": "tpl-webresources",
  "version": "0.1.0",
  "private": true,
  "description": "Web resources (TypeScript, módulos ES + bundle) do template D365 CE — prefixo tpl.",
  "scripts": {
    "build": "esbuild src/account/form.ts --bundle --format=iife --global-name=Tpl --outfile=dist/account/form.js",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/xrm": "^9.0.0",
    "esbuild": "^0.20.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  }
}
