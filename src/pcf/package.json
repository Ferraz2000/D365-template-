{
  "name": "tpl-pcf",
  "version": "0.1.0",
  "private": true,
  "description": "Controles PCF (TypeScript). Lógica pura testável; o controle real usa pac pcf init.",
  "scripts": {
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  }
}
